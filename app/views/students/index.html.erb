<div class="students index">
  <h2>Students</h2>

  <%= link_to("Add New Student", new_student_path, :class => 'action new') %>

  <table class="listing" summary="Subject list" align="center">
    <tr class="header">
      <th>Profile Picture</th>
      <th>#</th>
      <th>Student Name</th>
      <th>Student Number</th>
      <th>Email</th>
      <th>Actions</th>
    </tr>

    <% @students.each do |student| %>
    <tr>
      <td><%= link_to "Download Profile Picture", student.profile_pic_url %></td>
      <td><%= student.id %></td>
      <td><%= student.first_name + " " + student.last_name %></td>
      <td><%= student.student_number %></td>
      <td><%= student.email %></td>
      <td class="actions" align="center">
        <%= link_to("Show", student_path(student), :class => 'btn btn-primary') %>
        <%= link_to("Edit", edit_student_path(student), :class => 'btn btn-warning') %>
        <%= link_to("Delete", delete_student_path(student), :class => 'btn btn-danger') %>
      </td>
    </tr>
    <% end %>
  </table>
</div>
<br /><br /><br /><br /><br /><br />
